"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Send } from "lucide-react";
export function VideoSubmissionForm({ endpoint, isAdmin = false }: { endpoint: string; isAdmin?: boolean }) { const router = useRouter(); const [form, setForm] = useState({ title: "", description: "", videoUrl: "", thumbnail: "", isVertical: false }); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const submit = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(""); const res = await fetch(endpoint, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await res.json(); setLoading(false); if (!res.ok) return setError(data.error || "Envoi impossible"); setForm({ title: "", description: "", videoUrl: "", thumbnail: "", isVertical: false }); setMessage(isAdmin ? "Reportage publié." : "Reportage envoyé à l’administration pour validation."); router.refresh(); }; return <form onSubmit={submit} className="lp-form-shell mb-7 space-y-4"><div><h2 className="font-bold">{isAdmin ? "Publier un reportage vidéo" : "Soumettre un reportage vidéo"}</h2><p className="mt-1 text-sm text-lp-gray">Collez un lien YouTube, Facebook, Instagram, TikTok ou Vimeo. {isAdmin ? "Publication immédiate." : "Le super-admin devra l’approuver."}</p></div><input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Titre du reportage" className="lp-form-input" /><textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Courte description" className="lp-form-input" rows={3} /><input required type="url" value={form.videoUrl} onChange={(e) => setForm({ ...form, videoUrl: e.target.value })} placeholder="https://www.youtube.com/watch?v=..." className="lp-form-input" /><input type="url" value={form.thumbnail} onChange={(e) => setForm({ ...form, thumbnail: e.target.value })} placeholder="Image de couverture (facultative)" className="lp-form-input" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.isVertical} onChange={(e) => setForm({ ...form, isVertical: e.target.checked })} /> Format vertical (Reel / Short)</label>{error && <p className="text-sm text-red-700">{error}</p>}{message && <p className="text-sm text-green-700">{message}</p>}<button disabled={loading} className="lp-btn-accent"><Send className="h-4 w-4" />{loading ? "Envoi…" : isAdmin ? "Publier le reportage" : "Envoyer pour approbation"}</button></form>; }
